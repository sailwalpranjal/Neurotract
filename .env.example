# Frontend: public URL of the FastAPI service
NEXT_PUBLIC_API_URL=http://localhost:8000

# Backend: comma-separated browser origins allowed to call the API
CORS_ORIGINS=http://localhost:3000

# Backend: writable directory for uploads, job state, and generated output
# Point this at a persistent volume in deployment.
NEUROTRACT_RUNTIME_DIR=.
